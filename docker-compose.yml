version: '3.9'
services:
  paper-mill-billing:
    build: .
    ports:
      - "8080:8080"
    environment:
      # JAVA_OPTS can be used to set memory limits or other JVM options at run time.
      - JAVA_OPTS=
    # Uncomment the section below to add a local Kafka broker for a real
    # messaging environment.  For this demo it remains commented.
    #depends_on:
    #  - kafka

  # Kafka broker configuration (optional).  Uncomment to enable.
  #kafka:
  #  image: bitnami/kafka:3.7
  #  environment:
  #    - KAFKA_ENABLE_KRAFT=yes
  #    - KAFKA_CFG_NODE_ID=1
  #    - KAFKA_CFG_PROCESS_ROLES=broker,controller
  #    - KAFKA_CFG_CONTROLLER_LISTENER_NAMES=CONTROLLER
  #    - KAFKA_CFG_LISTENERS=PLAINTEXT://:9092,CONTROLLER://:9093
  #    - KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://kafka:9092
  #    - KAFKA_CFG_CONTROLLER_QUORUM_VOTERS=1@kafka:9093
  #  ports:
  #    - "9092:9092"